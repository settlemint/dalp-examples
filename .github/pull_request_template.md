## What

<!-- One-paragraph description of the change. -->

## Why

<!-- Motivation, linked issue, etc. -->

## Checklist

- [ ] Includes a changeset (`bun run changeset`) if a published package or app changed
- [ ] `bun run lint` and `bun run typecheck` pass locally
- [ ] If a new example was added, it has its own `vercel.json`, `.env.example`, README with a Deploy button, and is listed in the root README
